// Validate the list
int valid_ip_list(char* ips[]) {
  return 1;
}

//------------------------------------------------------------------------------

// Copy the list
void copy_list(char* ips[]) {
  return;
}

//------------------------------------------------------------------------------

// Remove the dots from the copies
int to_int(char* ip) {
  return 1;
}

//------------------------------------------------------------------------------

// Create assocaitive array between
// dotted and non-dotted versions
void associative_array(char* str_ips[], int* int_ips[]) {
  ;
}

//------------------------------------------------------------------------------

// Sort the non-dotted version
void merge(int* ips[], int len) {
  ;
}

//------------------------------------------------------------------------------

void merge_sort(int* ips[], int len) {
  ;
}

//------------------------------------------------------------------------------

// Print dotted version in order
void display_ips(char* ips[], int len) {
  ;
}

//------------------------------------------------------------------------------

/**
 * Sorts a list of ip
 * addresses in ascending order
 */
int main() {
  return 0;
}
